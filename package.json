{
  "name": "@marlow/root",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run --stream dev --scope @marlow/pokedex"
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
